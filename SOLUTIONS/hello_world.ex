defmodule HelloWorld do
  def hello do
    IO.puts "Hello World"
  end
end

"""" 
To avoid the clutter of a full on elixir project (created by running `mix new ModuleName`)/
I have just added the ex file

To run:

Enter the elixir Interpreted: `iex`
Type:                         `c("hello_world.ex")`
                              `hello_world.hello`
""""
